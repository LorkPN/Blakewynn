namespace Assets._Project.Scripts.Data.Enums
{
    public enum TargetPosition
    {
        Front, // the frontmost unit in the lane
        Back,  // the backmost unit in the lane
        Random, // a random unit in the lane
        All, // all units in the lane
    }
}